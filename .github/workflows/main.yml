on: [push]

name: 'HTML Preview Action'
jobs:
  html_preview:
    runs-on: ubuntu-latest
    name: HTML Preview
    steps:
      - uses: actions/checkout@v2
      - name: Get HTML preview link
        uses: ./ci/test-action
